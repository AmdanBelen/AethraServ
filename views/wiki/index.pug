doctype html
html
  include ../head.pug
  body
    include ../simple_header.pug
    section#one.main.style1.special
      .container
        p #{title}
        |Records:
        br
        if records
          a(href=full_path) #{title}
          br
    include ../footer.pug