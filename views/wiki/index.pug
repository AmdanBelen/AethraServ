doctype html
html
  include head
  body
    include header
    section#one.main.style1
      .container
        h2 #{title}
        |Records:
        br
        if records
          a(href=full_path) #{title}
          br
    include ../footer.pug