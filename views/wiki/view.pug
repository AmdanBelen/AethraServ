doctype html
html
  include head
  body
    include header
    section#one.main.style1
      .container
         #content
          article
            if exists
              div.article!=html
              br
              a(href="/" + name + "/edit") Edit 
            else
              a(href="/" + name + "/edit") Create a new post
    include ../footer.pug